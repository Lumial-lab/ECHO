# Рій → кандидат по вузлу W4

**Вузол:** W4 Рій: ембеддинг-індекс тверджень графа — агент шукає 'схоже вже пробували' косинусно
**Модель:** deepseek(OpenRouter)
**Час:** 20260909_030335
**Статус:** CANDIDATE — чекає воріт Клавра (прийняти/відхилити/переробити)

---

### КРОК 0 — РАМКА  
Задача сформульована коректно: потрібно реалізувати мінімальний механізм для пошуку схожих тверджень у графі через косинусну схожість ембеддингів, щоб уникнути повторення вже провалених/доведених шляхів.  

---

### КРОК 1 — ДЕКОМПОЗИЦІЯ  
1. **SUBNODE: Вибір ембеддера** | Визначити оптимальний легкий ембеддер для текстів наукових тверджень | deps=доступність моделі, швидкість, якість кластеризації  
2. **SUBNODE: Індексація** | Створити індекс попередніх тверджень графа (з контекстом) | deps=формат даних графа, схема зберігання  
3. **SUBNODE: Пошук і звірка** | Реалізувати косинусний пошук + порогове значення для "схожості" | deps=вибір метрики, історичні дані  
4. **SUBNODE: Стенд тестування** | Мінімальний приклад з 10-20 твердженнями для перевірки роботи | deps=наявність тестових даних  

---

### КРОК 2 — РЕЗУЛЬТАТ  

#### 1) Відповідь:  
- **Ембеддер**: Використати `all-MiniLM-L6-v2` (легка, досить точна для наукових текстів, підтримує косинусну схожість).  
- **Індекс**: FAISS або Annoy для швидкого пошуку (зберігати вектори тверджень + їх метадані: статус "доведено"/"провалено").  
- **Звірка**: При `take` — кодувати нове твердження, шукати топ-3 найближчих (поріг схожості >0.85), показувати користувачу.  

#### 2) Хід міркування:  
- Клавр вже має KlavrMem — інтегруємо його для ембеддингів.  
- Для індексу вибрано FAISS через оптимізацію під векторний пошук.  
- Поріг 0.85 емпірично (на основі аналогічних NLP-задач).  

#### 3) Емпірична перевірка:  
```python
# Приклад коду для тесту:
from sentence_transformers import SentenceTransformer
import numpy as np

model = SentenceTransformer('all-MiniLM-L6-v2')
claims = ["Гіпотеза: когнітивні агенти здатні до мета-навчання", ...]  # 10-20 тестових тверджень
embeddings = model.encode(claims)

# Перевірка схожості між першим і другим твердженням
cos_sim = np.dot(embeddings[0], embeddings[1]) / (np.linal
