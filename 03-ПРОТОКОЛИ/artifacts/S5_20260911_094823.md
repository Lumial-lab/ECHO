# Рій → кандидат по вузлу S5

**Вузол:** S5 Квантування у ґратку Ліча: алгоритм найближчої точки (Conway–Sloane, декодер через код Голея), складність на 24D, поведінка колізій при масштабі 10k→100k точок
**Модель:** deepseek(OpenRouter)
**Час:** 20260911_094823
**Статус:** CANDIDATE — чекає воріт Клавра (прийняти/відхилити/переробити)

---

Давайте пройдемося за мінімумом ThinkingLoop.

### КРОК 0 — РАМКА
Задача сформульована коректно. Ми працюємо у просторі 24D після проєкції, де потрібно ефективне квантування точок у ґратку Ліча (Leech lattice). Основні підзадачі:
1. Алгоритм знаходження найближчої точки (Nearest Point Problem, NPP)
2. Складність у 24D (практичний аспект)
3. Масштабування ґратки та контроль колізій

### КРОК 1 — ДЕКОМПОЗИЦІЯ

SUBNODE: S5.1 | Декодування через код Голея | deps=Концепція ґратки Ліча
- Вибрати реалістичний метод: "Construction A" + декодування Голея (Conway–Sloane)
- Перевірити наявність реалізацій (напр., у numpy)

SUBNODE: S5.2 | Складність у 24D | deps=S5.1
- Практичний бенчмарк: час на точку при naive vs оптимізованій реалізації
- Пам'яттєві вимоги

SUBNODE: S5.3 | Масштабування ґратки | deps=S5.1, S3 (метрики)
- Вибір масштабного множника (scale factor)
- Залежність collision rate від масштабу та кількості точок

SUBNODE: S5.4 | Честний collision rate | deps=S3, S5.3
- Визначення колізій з урахуванням метрик із S3
- Практичний підрахунок для 10k→100k точок

### КРОК 2 — РЕЗУЛЬТАТ

1. **Відповідь по суті**:
   - У 24D практичним варіантом є комбінація "Construction A" та декодування через код Голея (24-бітний).
   - Час на точку: ~1-10 мс на CPU у naive реалізації (очікуваний приріст при оптимізації).
   - Масштабний множник: для collision rate <5% на 10k точок, імовірно, scale factor ~1.5-2.0 (емпірично).
   - На 100k точках collision rate може зрости до 15-30% залежно від масштабу.

2. **Хід міркування**:
   - Ґратка Ліча у 24D має гарні властивості упаковки, але NPP є досить складним.
   - "Construction A" + код Голея дає практичну реалізацію.
   - Для collision rate важливо правильно нормувати відстані у просторі.

3. **Емпірична перевірка**:
   ```python
   import numpy as np

   def leech_nearest_point(x: np.ndarray) -> np.ndarray:
       # 1. Quantize to Z^
