# Рій → кандидат по вузлу S2

**Вузол:** S2 Лінійні vs нелінійні проєктори (PCA/SVD vs AE 384->128->24): чи вартий нелінійний приріст ціни
**Модель:** deepseek(OpenRouter)
**Час:** 20260910_034826
**Статус:** CANDIDATE — чекає воріт Клавра (прийняти/відхилити/переробити)

---

### КРОК 0 — РАМКА
Задача сформульована коректно, але варто додати специфікацію метрик порівняння (наприклад, rank-based accuracy, збереження k-NN сусідства, час інференсу). Без цього "вартість приросту" залишається суб'єктивною категорією.

**Уточнена постановка**:  
*S2: Порівняти лінійні (PCA/SVD) та нелінійні (Autoencoder 384->128->24) методи стиснення на нашому датасеті KlavrMem (10k) за метриками:  
1) Відновлення топ-5 сусідів (Recall@5)  
2) Час інференсу на 1к вкладень  
3) Використання GPU/CPU пам'яті*

### КРОК 1 — ДЕКОМПОЗИЦІЯ  
**SUBNODES**:  
1. **S2a | Збір бейзлайнів PCA/SVD** | deps=S1  
   - Реалізувати PCA/SVD зі склейкою їх виходів (наприклад, PCA->16 + SVD->8) для сумарно 24D  
2. **S2b | Налаштування AE з bottleneck 24D** | deps=S1  
   - Побудувати AE з шатами [384-256-128-24] та вибрати фінальну активацію (tanh/None)  
3. **S2c | Валідація на KlarMem** | deps=S2a,S2b  
   - Перевірити збереження відношень для тверджень, де контрастивні втрати S1 працювали  
4. **S2d | Кількісне порівняння** | deps=S2c  
   - Заміряти метрики для рішення "в production" (час/пам’ять/якість)  

---  
### КРОК 2 — РЕЗУЛЬТАТ  
**1) Висновок**:  
Нелінійний проєктор (AE) має сенс тестувати *лише* якщо:  
- У S1 виявили нелінійні залежності в даних (наприклад, кластеризація у PCA показує "розмиті" групи)  
- Достатньо GPU-ресурсів для ~3x уповільнення інференсу  

**2) Хід міркування**:  
- **PCA/SVD**: дають лінійне найкраще наближення за MSE, але погано зберігають локальні сусідства ([докази з R5a])  
- **AE**: можуть виявити неявні многовиди, але вимагають налаштування глибини/активацій  

**3) Емпірична перевірка**:  
```python
# Для S2a (PCA/SVD)
from sklearn.decomposition import PCA, TruncatedSVD
pca = PCA(n_components=16).fit(embeddings)
svd = TruncatedSVD(n_components=8).fit(
